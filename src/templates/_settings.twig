{% import "_includes/forms" as forms %}

{{ forms.textField({
    first: true,
    label: "Waitwhile API Key",
    name: 'api_key',
    value: settings.api_key
}) }}

{{ forms.textField({
    first: true,
    label: "Waitwhile Waitlist ID",
    name: 'waitlist_id',
    value: settings.waitlist_id
}) }}